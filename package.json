{
  "name": "mnrega_visualizations",
  "version": "1.0.0",
  "description": "Root package for MNREGA Visualizations project",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "heroku-postbuild": "npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "postinstall": "npm --prefix backend install && npm --prefix frontend install --include=dev"
  },
  "engines": {
    "node": ">=18"
  }
}
